source "https://rubygems.org"

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.3.0"

# Rails framework
gem "rails", "~> 7.0.0"

# Database adapters (uncomment the one you'll use)
gem "pg", "~> 1.1"        # PostgreSQL
# gem "mysql2", "~> 0.5"  # MySQL
# gem "sqlite3", "~> 1.6" # SQLite for development

# QueryGuard (development with local path)
gem "query_guard", path: ".."

group :development, :test do
  gem "rspec-rails"
end
